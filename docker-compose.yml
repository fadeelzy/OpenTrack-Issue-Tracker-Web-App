

services:
  web:
    build: .
    container_name: opentrack_web
    command: gunicorn openproject.wsgi:application --bind 0.0.0.0:$PORT
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always
